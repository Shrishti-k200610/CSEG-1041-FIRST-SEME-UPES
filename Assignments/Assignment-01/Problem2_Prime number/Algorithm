START:
INPUT: N
CHECK: if N<=1, print "NOT PRIME"
Else, run a loop from i = 2 to N/2
Inside the roop, CHECK: if N % i == 0, print "NOT PRIME"
End the loop
if loop ends without finding a divisor, print "PRIME"
STOP
