START
INPUT N (size of array) and arr (array elements)
INITIALISE a TEMPORARY VARIABLE temp=0
RUN a Nested Loop from i=0 to N-1 and inside it from j=0 to N-i-1
Inside it CHECK if arr[j] > arr[j+1]
IF yes, then SWAP them.
Display the sorted array
STOP
